name: Publicação automática
on:  
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Cria um arquivo temporário para a chave privada e altera a permissão do arquivo
        run: |
          echo "${{ secrets.KEY }}" > /tmp/private_key
          chmod 600 /tmp/private_key

      - name: Criando a pasta da aplicação no servidor
        run: |
          ssh -i /tmp/private_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null ubuntu@${{ secrets.HOST }} \
            "sudo mkdir -p /home/ubuntu/apps/helloflask"

      - name: Ajustar permissões no servidor de Produção
        run: |
          sshx -i /tmp/private_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null ubuntu@${{ secrets.HOST }} \
            "sudo chown -R ubuntu:ubuntu /home/ubuntu/apps && sudo chmod -R 755 /home/ubuntu/apps"

      # Backup da aplicação atual antes de substituir
      - name: Backup da aplicação atual
        run: |
          ssh -i /tmp/private_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null ubuntu@${{ secrets.HOST }} \
            "cd /home/ubuntu/apps && tar -czf /home/ubuntu/apps/helloflask_backup_$(date +'%Y%m%d%H%M%S').tar.gz helloflask"

      - name: Transferindo os arquivos da aplicação
        run: |
          scp -i /tmp/private_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -r ${{ github.workspace }}/* \
            ubuntu@${{ secrets.HOST }}:/home/ubuntu/apps/helloflask

      - name: Criando e ativando ambiente virtual e instalando as dependências
        run: |
          ssh -i /tmp/private_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null ubuntu@${{ secrets.HOST }} << 'EOF'
            cd /home/ubuntu/apps/helloflask
            python3 -m venv .venv
            source .venv/bin/activate
            pip install --upgrade pip
            pip install flask gunicorn
          EOF

      - name: Reiniciando o serviço da aplicação
        run: |
          ssh -i /tmp/private_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null ubuntu@${{ secrets.HOST }} \
            sudo systemctl restart helloflask.service

      - name: Limpa o arquivo de chave privada após uso
        run: |
          rm /tmp/private_key

      # Notificação de sucesso (já configurada)
      - name: Notificação de sucesso para Discord
        if: success()
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        uses: Ilshidur/action-discord@master
        with:
          args: '🚀 Deploy concluído e aplicação reiniciada com sucesso! ✅\n\nAutor do push: **${{ github.actor }}**'

      # Notificação de erro para Discord
      - name: Notificação de falha para Discord
        if: failure()
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        uses: Ilshidur/action-discord@master
        with:
          args: '❌ O deploy falhou! 😞\n\nAutor do push: **${{ github.actor }}**'
